name: example
description: Example app for span_builder

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  span_builder:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  span_builder_test:
    path: ../../span_builder_test

flutter:

  uses-material-design: true

